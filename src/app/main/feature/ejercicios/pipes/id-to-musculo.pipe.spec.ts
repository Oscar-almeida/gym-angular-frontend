import { IdToMusculoPipe } from './id-to-musculo.pipe';

describe('IdToMusculoPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToMusculoPipe();
    expect(pipe).toBeTruthy();
  });
});
